{% extends '::base.html.twig' %}

{% block title %}{{ parent() }} - Ajouter un post{% endblock %}

{% block body %}

    <a href="{{ app.request.headers.get('referer') }}">Retour</a>

    <h1>Ajouter un post</h1>

    <div>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}
    </div>

{% endblock %}
